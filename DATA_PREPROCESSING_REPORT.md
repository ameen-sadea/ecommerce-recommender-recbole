# 数据预处理报告

本文档面向课程报告撰写，说明本实验使用的数据规模、稀疏性、划分方式、格式转换方法以及序列推荐数据的构造过程。文中数值对应 `Movies_and_TV` 单域推荐实验。

## 1. 数据集概况

本实验使用电商场景下的用户-商品交互数据。每条记录表示一个用户与一个商品发生过交互。实验将推荐任务建模为隐式反馈推荐，即只关心用户是否与商品发生过交互，不使用显式评分大小作为主要监督信号。

处理后的数据规模如下：

```text
+----------------------+----------------+
| 项目                 | 数值           |
+----------------------+----------------+
| 用户数               | 657,204        |
| 商品数               | 197,944        |
| Train 交互数         | 6,126,723      |
| Valid 交互数         | 657,203        |
| Test 交互数          | 657,203        |
| 总交互数             | 7,441,129      |
+----------------------+----------------+
```

从划分结果看，验证集和测试集基本保持每个用户各一条目标交互，训练集保留用户更早的历史交互。这种划分适合评估“基于历史行为预测下一个商品”的推荐任务。

## 2. 稀疏度分析

推荐系统中的用户-商品矩阵通常极其稀疏。若将所有用户和所有商品构成完整矩阵，则矩阵大小为：

```text
657,204 × 197,944 = 130,104,191,376
```

实际观测到的总交互数为 `7,441,129`，因此整体密度为：

```text
density = 7,441,129 / 130,104,191,376
        ≈ 0.00005720
        ≈ 0.00572%
```

整体稀疏度为：

```text
sparsity = 1 - density
         ≈ 99.99428%
```

训练矩阵只使用 `Train` 交互，训练密度和稀疏度为：

```text
train density  ≈ 0.00471%
train sparsity ≈ 99.99529%
```

平均交互情况：

```text
+----------------------------+-----------+
| 指标                       | 数值      |
+----------------------------+-----------+
| 平均每用户总交互数         | 11.32     |
| 平均每用户训练交互数       | 9.32      |
| 平均每商品总交互数         | 37.59     |
+----------------------------+-----------+
```

这说明数据存在典型的长尾推荐问题：用户历史较短、商品空间较大、绝大多数用户-商品组合没有交互记录。因此实验中不仅需要普通采样评估，还额外补充了全库排序评估，以观察模型在约 20 万商品候选空间下的真实排序能力。

## 3. 划分方式

数据已经在预处理前被划分为：

```text
+-------------+---------------------------+----------+---------------------------+
| 划分        | 文件                      | 行数     | 含义                      |
+-------------+---------------------------+----------+---------------------------+
| Train       | train.csv                 | 6,126,723| 用户历史交互              |
| Valid       | valid.csv                 | 657,203  | 每用户验证目标            |
| Test        | test.csv                  | 657,203  | 每用户测试目标            |
+-------------+---------------------------+----------+---------------------------+
```

实验采用类似 leave-one-out 的思想：对每个用户保留较早交互作为训练历史，将后续交互作为验证和测试目标。这样可以避免随机划分破坏用户行为序列，也更接近推荐系统中“用过去预测未来”的使用场景。

在模型训练阶段：

- `Train` 用于学习用户和商品表示。
- `Valid` 用于选择最优 epoch 和早停。
- `Test` 只用于最终指标汇报。

## 4. 字段处理

原始 CSV 至少包含两列：

```text
+-----------+------------------------------+
| 字段      | 说明                         |
+-----------+------------------------------+
| user_id   | 用户唯一标识                 |
| item_id   | 商品唯一标识                 |
+-----------+------------------------------+
```

可选字段：

```text
+-----------+------------------------------+
| 字段      | 处理方式                     |
+-----------+------------------------------+
| rating    | 缺失时填充为 1.0             |
| timestamp | 缺失时填充为 0               |
+-----------+------------------------------+
```

处理规则：

- `user_id` 和 `item_id` 统一转为字符串 token，避免原始 ID 类型差异导致映射错误。
- `rating` 转为浮点数；本实验主要按隐式反馈处理，因此默认值为 `1.0`。
- `timestamp` 转为整数，用于序列模型中保持用户行为顺序。
- 列名大小写不敏感，便于兼容不同预处理输出。

## 5. 格式转换

RecBole 要求输入数据使用 atomic file 格式，字段名需要携带类型声明。本项目将 CSV 转换为 `.inter` 文件。

转换前：

```text
train.csv / valid.csv / test.csv
```

转换后：

```text
datasets/movies_tv/
+-- movies_tv.train.inter
+-- movies_tv.valid.inter
+-- movies_tv.test.inter
```

转换后的表头为：

```text
user_id:token    item_id:token    rating:float    timestamp:float
```

其中 `token` 表示离散 ID，`float` 表示数值字段。文件实际使用 tab 分隔。

这一步只改变文件格式，不改变样本数量，不重新划分数据，也不主动过滤用户或商品。

## 6. 模型使用的数据形式

不同模型使用的数据形式不同：

```text
+--------------------------+----------------------------+------------------------------+
| 模型                     | 数据形式                   | 数据目录                     |
+--------------------------+----------------------------+------------------------------+
| Pop                      | 扁平交互                   | datasets/movies_tv           |
| ItemKNN                  | 扁平交互                   | datasets/movies_tv           |
| BPR                      | 扁平交互                   | datasets/movies_tv           |
| NeuMF                    | 扁平交互                   | datasets/movies_tv           |
| LightGCN                 | 扁平交互图                 | datasets/movies_tv           |
| CrossDomainNeuMF         | 扁平交互 + 自定义采样      | datasets/movies_tv           |
| SASRec / BERT4Rec        | 用户历史序列               | datasets/movies_tv_seq       |
+--------------------------+----------------------------+------------------------------+
```

扁平交互数据适用于矩阵分解、图协同过滤和常规神经协同过滤模型。序列模型则需要额外构造 `item_id_list`，用于表示用户在目标 item 之前的历史行为。

## 7. 序列数据构造

为了训练 SASRec 和 BERT4Rec，需要将每个用户的历史交互转换成序列样本。序列数据包含：

```text
+--------------+----------------------------------------------+
| 字段         | 含义                                         |
+--------------+----------------------------------------------+
| user_id      | 用户 ID                                      |
| item_id      | 当前预测目标 item                            |
| item_id_list | 目标发生前的历史 item 序列                  |
| item_length  | 历史序列长度                                 |
+--------------+----------------------------------------------+
```

序列样本构造方式如下。

### 训练集序列

对用户训练历史 `i1, i2, i3, i4, ...`，从第二个 item 开始构造训练样本：

```text
+----------+----------------+--------+
| 样本     | item_id_list   | item_id|
+----------+----------------+--------+
| 第 1 条  | i1             | i2     |
| 第 2 条  | i1 i2          | i3     |
| 第 3 条  | i1 i2 i3       | i4     |
+----------+----------------+--------+
```

如果设置最大历史长度 `K`，则只保留最近 `K` 个历史 item。

### 验证集序列

验证时使用：

```text
历史 = train 中最后 K 个 item
目标 = valid 中该用户的 item
```

### 测试集序列

测试时使用：

```text
历史 = train 历史 + valid item
目标 = test 中该用户的 item
```

这种设置保证测试目标不会泄漏到输入历史中，同时允许模型使用验证目标之前的完整可见历史。

## 8. 序列长度设置

实验中比较了不同最大历史长度：

```text
+--------+--------------------------+------------------------------+
| 设置   | 数据集                   | 说明                         |
+--------+--------------------------+------------------------------+
| K=50   | movies_tv_seq            | SASRec 主实验设置            |
| K=5    | movies_tv_seq_k5         | 短历史消融                   |
| K=2    | movies_tv_seq_k2         | 极短历史消融                 |
+--------+--------------------------+------------------------------+
```

从实验结果看，K=50 的 SASRec 在验证集、测试集和全库评估中均优于 K=5、K=2，说明在该数据集上更长的用户行为历史对序列推荐有正向作用。

## 9. 负采样与评估候选集

训练和评估阶段使用了两类候选集设定：

```text
+----------+----------------------------+------------------------------+
| 协议     | 候选集                     | 用途                         |
+----------+----------------------------+------------------------------+
| uni100   | 1 个正例 + 100 个负例      | 主结果表、快速对比           |
| 全库     | 约 197,944 个商品全排序    | 更接近真实推荐场景           |
+----------+----------------------------+------------------------------+
```

`uni100` 评估计算速度快，适合多模型横向比较；但它只在少量负例中排序，指标通常会高于全库排序。全库评估需要在约 20 万商品中找到目标 item，因此指标更低，但更能反映真实推荐难度。

## 10. 预处理后的文件结构

完整预处理完成后，本地会出现：

```text
datasets/
+-- movies_tv/
|   +-- movies_tv.train.inter
|   +-- movies_tv.valid.inter
|   +-- movies_tv.test.inter
+-- movies_tv_seq/
|   +-- movies_tv_seq.train.inter
|   +-- movies_tv_seq.valid.inter
|   +-- movies_tv_seq.test.inter
+-- movies_tv_seq_k5/
|   +-- movies_tv_seq_k5.train.inter
|   +-- movies_tv_seq_k5.valid.inter
|   +-- movies_tv_seq_k5.test.inter
+-- movies_tv_seq_k2/
    +-- movies_tv_seq_k2.train.inter
    +-- movies_tv_seq_k2.valid.inter
    +-- movies_tv_seq_k2.test.inter
```

其中 `movies_tv` 是所有非序列模型的基础数据，`movies_tv_seq*` 是序列模型专用数据。

## 11. 预处理方法小结

本实验的数据预处理可概括为四步：

```text
+------+--------------------------+--------------------------------+
| 步骤 | 操作                     | 目的                           |
+------+--------------------------+--------------------------------+
| 1    | 整理 train/valid/test    | 固定评估协议，避免随机划分波动 |
| 2    | CSV 转 RecBole .inter    | 适配 RecBole 数据读取格式      |
| 3    | 构造序列样本             | 支持 SASRec/BERT4Rec           |
| 4    | 设置负采样和全库评估     | 同时保证可比性和真实性         |
+------+--------------------------+--------------------------------+
```

该处理方式保留了用户行为的时间顺序，避免测试目标泄漏，并将同一份数据同时适配传统协同过滤、图推荐、神经协同过滤和序列推荐模型。

## 12. 报告中可直接使用的表述

本实验使用 Movies_and_TV 单域用户-商品交互数据。预处理后共有 657,204 个用户、197,944 个商品和 7,441,129 条交互。数据按照用户历史顺序划分为训练集、验证集和测试集，其中训练集包含 6,126,723 条交互，验证集和测试集各包含 657,203 条交互。用户-商品交互矩阵整体密度约为 0.00572%，稀疏度约为 99.99428%，具有典型的大规模稀疏推荐特征。

为了适配 RecBole 框架，实验将原始 CSV 转换为带字段类型声明的 `.inter` atomic file。普通推荐模型使用 `user_id`、`item_id`、`rating`、`timestamp` 四列交互数据；序列推荐模型额外构造 `item_id_list` 和 `item_length` 字段，将用户目标交互之前的历史商品序列作为输入。实验保留每个用户的时间顺序，使用训练历史预测验证和测试目标，从而避免未来信息泄漏。

评估阶段采用两种协议：主表使用 `1 正例 + 100 负例` 的 uni100 协议，以便高效比较多个模型；附加实验使用约 197,944 个商品的全库排序协议，更接近真实推荐系统的候选空间。两类协议共同反映模型在采样评估和真实大规模排序场景下的表现。
